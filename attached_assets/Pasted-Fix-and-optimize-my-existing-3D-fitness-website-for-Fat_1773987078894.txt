Fix and optimize my existing 3D fitness website for "Fat 2 Fit Gym" to work perfectly on mobile devices without breaking the desktop version.

Do NOT redesign the website. Improve responsiveness, performance, and layout for smaller screens.

📱 Mobile Optimization Tasks
1. Layout Fix

Fix all overflow issues (no horizontal scrolling)

Ensure all sections fit within screen width

Convert multi-column layouts into single-column on mobile

Add proper padding (16px–20px)

2. Typography Fix

Scale text properly:

Headings: responsive (clamp or smaller sizes)

Paragraphs: readable on small screens

Prevent text from overflowing or overlapping

3. 3D / Heavy Animations Fix (CRITICAL)

Disable or reduce heavy Three.js animations on mobile

Replace with:

Static image OR

Lightweight animation

Ensure no lag or frame drops

4. Navigation Fix

Convert navbar into hamburger menu

Add sticky bottom CTA bar:

“Join Now”

“WhatsApp”

Ensure buttons are thumb-friendly (min 44px height)

5. Section Adjustments
Hero Section:

Reduce height (avoid full heavy 3D load)

Keep CTA visible without scrolling

Trainers Section:

Convert grid → vertical cards or horizontal scroll

Services Section:

Stack cards vertically

Testimonials:

Make it swipeable slider

Pricing:

Stack cards (no side-by-side)

6. Performance Optimization

Lazy load images and sections

Compress assets (WebP)

Reduce animation duration

Remove unnecessary shadows/blur on mobile

7. Touch Optimization

Add tap feedback (scale animation)

Increase button spacing

Avoid hover-only interactions

8. Responsive Breakpoints

Use proper breakpoints:

Mobile: < 768px

Tablet: 768px–1024px

Desktop: > 1024px

9. Fix Common Issues

Fix elements overlapping

Fix broken flex/grid layouts

Fix z-index issues

Ensure images scale properly (object-fit: cover)

10. Keep Desktop Intact

Do NOT change desktop layout or animations

Only adjust for mobile using responsive design

⚙️ Tech Requirements

Use Tailwind responsive utilities (sm, md, lg)

Use flexbox/grid properly

Use conditional rendering for heavy components

🎯 Goal

The website should:

Look clean and modern on mobile

Load fast (under 3 seconds)

Have smooth scrolling

Be easy to navigate with one hand